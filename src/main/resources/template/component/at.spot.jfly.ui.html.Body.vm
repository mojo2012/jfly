<body uuid="$!{component.uuid()}" class="$!{component.getCssStyleString()} hidden" $!events>
	<v-app data-app >
		#foreach( $!child in $!component.children() )
			$!child.render()
		#end
	</v-app>
</body>